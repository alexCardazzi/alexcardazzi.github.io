---
title: "Homework 3-1"
author: "Your Name Here"
institute: "Old Dominion University"
format: 
  html:
    theme: lux # Check here for more themes: https://quarto.org/docs/output-formats/html-themes.html
    code-tools: true
    code-fold: true
    code-summary: "Code"
    code-copy: hover
    link-external-newwindow: true
    tbl-cap-location: top
    fig-cap-location: bottom

self-contained: true
editor: source
---

```{r setup, include=FALSE}
# DO NOT EDIT THIS

knitr::opts_chunk$set(fig.align = 'center')
knitr::opts_chunk$set(out.width = '90%')
knitr::opts_chunk$set(results = 'hold')
knitr::opts_chunk$set(fig.show = 'hold')
knitr::opts_chunk$set(error = TRUE)
knitr::opts_chunk$set(warning = FALSE)
knitr::opts_chunk$set(message = FALSE)
par(mar = c(4.1, 4.1, 1.1, 4.1))

Q <- 0
```

This homework explores the question, "do police reduce crime?" by replicating Di Tella & Schargrodsky ([2004](https://www.jstor.org/stable/3592772)). This study addresses this question by exploiting a terrorist attack on a Jewish institution in Argentina, after which all similar institutions received additional police for security. Please read through the paper before completing this Homework.^[The PDF will be made available on Canvas.]

### Question `r Q <- Q+1; Q`

Summarize the main research question in the paper, the application analyzed, and why DiD is appropriate for this research question. (**1 Point**)

*Your answer here.*


### Question `r Q <- Q+1; Q`

```{r include=FALSE}
q <- 0
```

`r q <- q+1; letters[q]`. Read in [`ditella_2004.csv`](https://alexcardazzi.github.io/econ708/data/ditella_2004.csv) and name it `argentina`. (**0 Points**)

```{r}
# Your code here.
```

`r q <- q+1; letters[q]`. Tabulate the `month_raw` variable. Notice the "extra" observations, and use code to eliminate those rows. Re-tabulate the `month_raw` variable to confirm. (**1 Point**)

```{r}
# Your code here.
```


`r q <- q+1; letters[q]`. Identify (in words) the variables indicating "treatment" and "post." If one (or both) do (does) not exist, construct them. (**0 Points**)

```{r}
# Your code here.
```


*Your answer here.*


`r q <- q+1; letters[q]`. Tabulate the number of observations in each of the following groups: treatment group pre-treatment, treatment group post-treatment, control group pre-treatment, control group post-treatment. (**1 Point**)

```{r}
# Your code here.
```

`r q <- q+1; letters[q]`. For each of the group-periods above, find the average number car_thefts. (**1 Point**)

```{r}
# Your code here.
```

`r q <- q+1; letters[q]`. Produce a visual of the average number of car thefts by treatment group over time. Be sure to include points, lines, a vertical line representing the timing of the terrorist attack, a legend, and axis labels. (**1 Point**)

```{r, fig.show='hold'}
# Your code here.
```

`r q <- q+1; letters[q]`. Notice that there are 3 neighborhoods contained within the data. Which variables vary by neighborhood? Produce a summary statistics table to address this, and provide a discussion. (**1 Point**)

```{r}
# Your code here.
```

*Your answer here.*

### Question `r Q <- Q+1; Q`

```{r include=FALSE}
q <- 0
```

`r q <- q+1; letters[q]`. Discuss the general assumptions underlying the causal inference method used in the empirical analysis and how they apply in this research setting. (**1 Point**)


*Your answer here.*

`r q <- q+1; letters[q]`. Discuss threats to a causal interpretation of the results. How do the authors address these assumptions in the paper (if at all)? (**1 Point**)

*Your answer here.*

### Question `r Q <- Q+1; Q`

```{r include=FALSE}
q <- 0
```

`r q <- q+1; letters[q]`. Construct an indicator variable that measures whether a block is within two blocks of a Jewish institution (call is `jewish_inst_within_2_blocks`). (**0 Points**)

```{r}
# Your code here.
```


`r q <- q+1; letters[q]`. Replicate columns (A), (B), and (C) of Table 3. (**2 Points**)

```{r}
# Your code here.
```

`r q <- q+1; letters[q]`. How does the interpretation change as one moves from columns (A), (B), and (C)? (**1 Point**)

*Your answer here.*

`r q <- q+1; letters[q]`. Estimate and plot an event study version of column (A). Add a horizontal line equal to the treatment effect from (A). What does this figure tell you? (**2 Points**)

```{r}
# Your code here.
```

*Your answer here.*

`r q <- q+1; letters[q]`. Re-estimate column (C), except with a LPM. Interpret the coefficients quantitatively and the overall results qualitatively. (**2 Points**)

```{r}
# Your code here.
```

*Your answer here.*

### Question `r Q <- Q+1; Q`

```{r include=FALSE}
q <- 0
```

`r q <- q+1; letters[q]`. Test whether the treatment effect varies by neighborhood. (**1 Point**)

```{r}
# Your code here.
```

*Your answer here.*

`r q <- q+1; letters[q]`. Estimate a triple difference specification of column (A) with bank, public building, and gas station indicators like in Table 7. Interpret the coefficients. (**2 Points**)

```{r}
# Your code here.
```

*Your answer here.*

### Question `r Q <- Q+1; Q`

Do you believe the results? What would you have done differently or have liked to see tested? (**2 Points**)

*Your answer here.*


